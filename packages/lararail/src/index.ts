import { lararail } from './plugin'
import { current, defineRoutes, route, RouterGlobal } from './router'
import store from './store'

export { current, defineRoutes, lararail, route, store }
export type { RouterGlobal }
